# Generated by scripts/bundle-pmu.mjs (runs on prepack). The emailer's bundled closure —
# JS modules, data, and the prebuilt daemon — is regenerated from the repo at publish time,
# so it ships in the tarball without bloating git history. Do not commit these.
/scripts/pmu/
/data/
/.thetacog/
/docs/
/src/

# --- standalone-repo additions ---
node_modules/
pmu-rust/target/
.DS_Store
*.log
npm-debug.log*
.env
.env.*
